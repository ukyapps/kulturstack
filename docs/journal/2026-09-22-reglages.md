---
type: journal
dates: 2026-09-22 (midi)
statut: PR 11 ouverte (#15), review locale PASS — dernière PR de la Tranche 1
---

# Session 13 — Réglages, Confidentialité, À propos (PR 11)

## Ce qui a été fait

- PR #14 (Envie) fusionnée.
- **PR 11** sur `feat/settings-about` :
  - `WipeUseCase` (Domain) sur `MediaRepository.deleteAll()` — objet par objet (le `delete(model:)` en masse de SwiftData échoue sur les relations obligatoires) ; `DemoSeed.wipe()` l'utilise aussi.
  - `Features/Settings/` : `SettingsView` (⚙︎ dans la barre du Journal) — Langue « Suit le système » + lien vers les Réglages iOS, Confidentialité, À propos, **Tout effacer** (rouge, footer, **double confirmation** : dialogue puis alerte), section DEBUG hors Release ; `SettingsViewModel` (`wipeAll()` → Bool + `didFailToWipe`, `versionLine`) ; `PrivacyView` (les deux paragraphes du TDD 07, « ce qui sort » / « ce qui reste ») ; `AboutView` (version + build, tagline, logo TMDB officiel en SVG vectoriel, mention obligatoire « n'est ni approuvé ni certifié », OpenLibrary).
  - `Debug/DebugSection.swift` remplace `DebugMenu` (coccinelle) et le badge de base du Journal : tout est dans Réglages. Libellé « Tout effacer (démo) » pour ne pas confondre avec le vrai.
  - **Bandeau hors-ligne** dans la Recherche : `ConnectivityMonitoring` (Domain), `NetworkMonitor` (Data, `NWPathMonitor`), injecté par `AppServices` ; `SearchViewModel.isOffline` ; `OfflineBanner` (design system) en `safeAreaInset` haut.
  - Icône provisoire : « K » blanc sur l'accent, 1024 px, sans transparence.
  - 27 clés FR / EN, 9 tests (195). Domain 96 %, Data 97 %, Features 87 %.
- Vérifié par la founder : Réglages, À propos (logo TMDB), Confidentialité. Capture EN du Journal (Week · 56, Want to, Today).

## Pièges

1. `context.delete(model:)` (suppression en masse) → « Batch delete failed due to mandatory OTO nullify inverse on LogEntry/item ». Boucle `fetch` + `delete`, logs d'abord.
2. `String(localized:locale:)` ne change pas de langue dans les tests (renvoie le FR pour `en`). Pour vérifier une traduction : `Bundle.main.path(forResource: "en", ofType: "lproj")` puis `localizedString(forKey:)`.

## Choix faits

- Deux confirmations pour « Tout effacer » : irréversible, et pas d'export avant T3.
- Le bandeau hors-ligne n'est pas testé sur le simulateur (couper le réseau n'est pas simple) ; le rendu avec un moniteur simulé l'est.
- Pas de « Import / Export » dans Réglages tant que T3 n'existe pas (le design le prévoyait masqué).

## Tranche 1 — bilan

14 PRs sur 14 (dont 8b et 8c ajoutées en route). **Le produit T1 est complet** : chercher → logger (ou garder en envie) → journal filtré → fiche → modifier → réglages. Prochaine étape : **TestFlight** (jalon, pas une PR) — compte développeur, bundle id, `PrivacyInfo.xcprivacy` déjà là, étiquette « Données non collectées ».

## À faire — founder

- [ ] Fusionner la PR #15 quand `test` est vert.
- [ ] Réserver `kulturstack.com` + `kulturstack.app`.
- [ ] Compte Apple Developer actif pour TestFlight ?

## À faire — prochaine session Claude

1. Jalon TestFlight : vérifier la checklist du plan (`docs/plans/tranche-1.md` § Jalon), archive, upload, notes de test.
2. Puis planifier la Tranche 2 (épisodes) — `docs/plans/tranche-2.md` à écrire.
