---
type: journal
dates: 2026-09-22 (midi)
statut: PR 10 ouverte (#14), review locale PASS
---

# Session 12 — Envie (PR 10)

## Décisions produit (founder, à l'écran)

1. **Les envies vivent à part** : le Journal et ses compteurs ne montrent que ce qui a été consommé.
2. **Un troisième onglet « Envie »** (Journal · Envie · Recherche), pas un chip du Journal — la question §6.5 du design est tranchée dans l'autre sens que la reco : « je veux pas d'un chip envie dans le journal, il faut faire un 3ᵉ onglet ».
3. **♡ à côté du + sur chaque résultat** de Recherche, pour garder en un geste (« comme le petit + pour logger, le petit cœur d'envie pour ajouter direct »). L'appui long est retiré : une seule façon de faire.

## Ce qui a été fait

- PR #13 (Journal par période) fusionnée.
- **PR 10** sur `feat/wishlist` :
  - `LogUseCase.wish(candidate)` / `wish(item)` → log `wishlist` ; `StatsUseCase.filter` / `count` excluent les envies ; `StatsUseCase.pendingWishes` = envies sans consommation postérieure de la même œuvre.
  - `Features/Wishlist/` : `WishlistViewModel` (loading · empty · loaded · failed ; `markSeen(id:)` = `logAgain(item)` ; `delete(id:)`), `WishlistView` (liste, état vide « Rien en attente » + Chercher, erreur), `WishRow` (jaquette, titre, sous-titre, « Ajouté le … », bouton « Je l'ai vu / lu / écouté / j'y ai joué » selon le type).
  - Recherche : ♡ + + en accessoires de ligne ; `SearchViewModel.wish(candidate)` → bandeau « Gardé pour plus tard ✓ · Modifier ».
  - Fiche : bouton « Envie » (♡) à côté de « Logger » ; `ItemDetailViewModel.wish()` (candidat → crée la fiche avec un log envie ; fiche existante → ajoute l'envie).
  - `RootView` : 3 onglets. 13 clés FR / EN, 15 tests (187). Domain 96 %, Features 85 %.
- Vérifié par la founder : onglet vide ; ♡ sur Oppenheimer → ligne « Ajouté le 22 sept. · Je l'ai vu » ; « Je l'ai vu » → Journal (Aujourd'hui), onglet Envie vide ; ♡ dans la liste de résultats → bandeau.

## Ce que la démo a révélé

- **Bug attrapé par la founder** : après « Je l'ai vu », l'envie restait dans l'onglet (je listais toutes les envies, fidèle à « l'envie reste dans l'historique » mais pas au sens de la liste d'attente). Corrigé par `pendingWishes` : en attente = pas de log consommé **postérieur** sur la même œuvre. Testé dans les deux sens (vu après → sort ; vu avant → reste).

## Choix faits

- « Je l'ai vu » sur un livre dit « Je l'ai lu », sur un disque « Je l'ai écouté » (`MediaKind.seenActionLabel`), comme la pastille « Vu le / Lu le ».
- Une envie n'affiche pas de pastille « Vu le … » dans la Recherche (pas une consommation) ; une future pastille « En envie » est possible, non demandée.
- Un chip « Envie » a existé une heure dans cette branche avant d'être remplacé par l'onglet : pas de trace dans le code, juste ici.

## À faire — founder

- [ ] Fusionner la PR #14 quand `test` est vert.
- [ ] Réserver `kulturstack.com` + `kulturstack.app`.

## À faire — prochaine session Claude

1. **PR 11** : Réglages (Langue suit le système, Tout effacer avec double confirmation, Confidentialité, À propos avec attributions TMDB + OpenLibrary), déplacer le menu DEBUG dans Réglages, bandeau hors-ligne de la Recherche, finitions. Dernière PR avant TestFlight.
